import { expect } from 'chai';
import { assert } from 'chai';
//import { assert }'mocha';
import 'mocha';

const fileJson = 'film.json';
const dir = './data/';

describe('Test file json', () => {
    it('is the file exist in the path ?', () => {

      //  expect(dir).to.be.a.directory().with.file

       // expect(dir).to.be.a.file();
       // assert.isFile(dir,fileJson);

    });
});

